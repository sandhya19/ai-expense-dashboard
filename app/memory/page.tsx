import { BrainCircuit, ShieldCheck } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { getReceiptMemory } from "@/lib/receipt-memory";

export default async function MemoryPage() {
  const memories = await getReceiptMemory();
  return <div className="mx-auto max-w-5xl space-y-7"><div className="flex items-start gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-primary/10 text-primary"><BrainCircuit className="size-6" /></span><div><p className="text-sm font-medium text-primary">Persistent memory</p><h1 className="mt-1 text-3xl font-semibold tracking-tight md:text-4xl">What ReceiptBrain remembers</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Each verified receipt becomes private context for the next useful answer, insight, and spending story.</p></div></div>{memories.length ? <div className="grid gap-4 sm:grid-cols-2">{memories.map((memory) => <Card key={memory.label}><CardContent className="p-5"><p className="text-sm font-medium text-primary">{memory.label}</p><p className="mt-2 text-xl font-semibold">{memory.value}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{memory.detail}</p></CardContent></Card>)}</div> : <Card className="border-dashed"><CardContent className="grid min-h-56 place-items-center p-6 text-center"><div><BrainCircuit className="mx-auto size-9 text-muted-foreground" /><p className="mt-3 font-medium">Your memory begins with a receipt.</p><p className="mt-1 text-sm text-muted-foreground">Upload and verify a receipt to give ReceiptBrain its first private memory.</p></div></CardContent></Card>}<Card><CardContent className="flex gap-3 p-5"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-emerald-600" /><p className="text-sm leading-6 text-muted-foreground">Memory is owner-scoped: it is built from your signed-in receipt history, and you can correct or delete any source receipt.</p></CardContent></Card></div>;
}
